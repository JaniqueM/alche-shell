#!/usr/bin/env bash
# Script: 8-beheaded_process
# Kills the 7-highlander process

# Find the PID of 7-highlander
PID=$(pgrep -f "7-highlander")

# Check if the PID exists
if [[ -n "$PID" ]]; then
    kill -9 "$PID"   # Send SIGKILL to forcefully terminate
fi
