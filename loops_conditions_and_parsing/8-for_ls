#!/usr/bin/env bash
# List current directory contents showing only part after first dash

for file in *; do
    # Skip if it's not a regular file/directory (optional safeguard)
    [ -e "$file" ] || continue
    # Remove everything up to and including the first dash
    echo "${file#*-}"
done
